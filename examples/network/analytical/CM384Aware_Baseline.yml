# Network Configuration
# Hierarchical topology: 4x8 2D Mesh at base layer + 3 layers of switches

# 4-layer topology: Mesh2D at bottom, then 3 layers of switches
#topology: [ Mesh2D, Switch, Switch, Switch ]
topology: [ Mesh1D, VirtualSwitch, SpinalSwitch]

npus_count: [ 2, 8, 48]

bandwidth: [ 270, 196, 196 ]  # GB/s

latency: [ 5.0, 400.0, 600.0 ]  # ns